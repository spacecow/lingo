<% if f.object.type == 'Japanese' %>
  <%= f.input :content, as: :text, label:false, placeholder:'Transcribe a section' %>
<% else %>
  <%= f.input :content, as: :text, label:false, placeholder:'Enter a translation' %>
<% end %>
<%# f.input :type, as: :hidden %>
