<%= simple_form_for [sentence, Comment.new] do |f| %>
  <%= f.input :content, label:false %>
  <div class="clear"></div>
  <%= f.button :submit %>
<% end if can? :create, Comment %>
<div class="clear"></div>

